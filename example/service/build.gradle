apply plugin: 'checkstyle'

configurations {
    checkstyleConfig
}

checkstyle {
    toolVersion = versions.checkstyle
    ignoreFailures = true
}

dependencies {
    compile deps.core
    checkstyleConfig deps.checkstyle
}
